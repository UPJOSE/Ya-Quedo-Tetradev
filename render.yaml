services:
  - type: web
    name: yaquedo-backend
    runtime: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        fromDatabase:
          name: yaquedo-db
          property: connectionString
      - key: DB_USERNAME
        fromDatabase:
          name: yaquedo-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: yaquedo-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://ya-quedo.vercel.app,https://ya-quedo-angular.vercel.app

databases:
  - name: yaquedo-db
    plan: free
    databaseName: yaquedo
    user: yaquedo_user
